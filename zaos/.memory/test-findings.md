# Test Run Findings — 2026-04-01

> Observations relevees pendant le premier vrai test run post-Phase 8.
> A traiter apres le test, dans une future phase.

## Issues & Ameliorations

| # | Categorie | Description | Priorite |
|---|-----------|-------------|----------|
| 1 | UX | Workflow Agents ne montre pas d'indicateur "actif" quand un agent est en cours de delegation — connecter agentsStore (runtime) avec workflowKitStore (config) pour afficher un badge/glow sur l'agent actif | moyenne |
| 2 | Workflow | Pas de transition brainstorming → pipeline : apres cadrage, Claude enchaine en mode free sans demander a passer en pipeline. Il faut une phase pre-pipeline de brainstorming structure (milestones avec l'user, pushback, precision des incertitudes) puis demande explicite de passage en pipeline. Ajouter dans CLAUDE.md un exemple complet de parcours projet vierge : brainstorming → milestones → demande pipeline → plan detaille → validation → plan d'implementation → recherche → code. Inclure exemples de pushback, questionnement des incertitudes, etc. | haute |
| 3 | Bug | Delegation history : chaque delegation apparait DEUX FOIS (pas un probleme de nommage, c'est bien des doublons). useStreaming cree probablement 2 entrees pour le meme tool_use_id (ex: une sur le content_block_start et une sur le content_block_delta). Verifier la dedup par tool_use_id dans agentsStore.addDelegation() | haute |
| 4 | Workflow | En mode free, le pipeline/gates/phases ne s'actualisent pas meme si Claude travaille correctement (delegue, code). Le dashboard pipeline reste "mort". Lie au finding #2 : Claude doit proposer le passage en pipeline au bon moment pour que le workflow state soit alimente et visible dans le dashboard | haute |
| 5 | UX | Delegation history : les agents actifs (running) devraient toujours etre affiches en haut de la liste, avant les completed/error. Trier par status puis par timestamp | basse |
| 6 | Feature | Pas de mode "accept edits" : l'user doit approuver chaque outil un par un. Deux besoins : (a) toggle "Accept Edits" (auto-approve Write/Edit UNIQUEMENT, toujours demander pour Bash et ops dangereuses — comme Shift+Tab dans Claude Code CLI) et (b) "Always allow" par outil specifique quand une commande se repete. PAS de --dangerously-skip-permissions par defaut. Implementer en interceptant les permission prompts cote Rust et auto-repondre selectivement. (c) Inclure WebSearch/WebFetch dans les outils auto-approve par defaut en mode "Accept Edits" — les agents researcher floodent de demandes d'autorisation pour chaque recherche web | haute |
| 7 | Bug | Dashboard memoire pas a jour en temps reel : les tasks ne se mettent a jour que quand Claude ecrit dans les fichiers memoire — et il ne le fait qu'a la fin (quand on lui dit d'arreter). Deux sous-problemes : (a) Claude doit mettre a jour current-epic.md APRES chaque task terminee, pas seulement en fin de session — renforcer dans CLAUDE.md + inject-context. (b) Verifier que le watcher + parser re-lisent bien quand le fichier change | haute |
| 8 | Workflow | Claude ne respecte pas vraiment le pipeline : pas d'agent reviewer spawne, pas d'attente de validation des gates pour enchainer entre phases. Il fonce de task en task sans pause. Le mode pipeline n'est pas enforce cote comportement — les gates et phases ne sont respectes que si Claude decide de les suivre. Renforcer : (a) instructions plus strictes dans CLAUDE.md (attendre gate entre chaque phase), (b) possiblement un mecanisme cote hook/backend qui bloque l'avancement sans gate valide, (c) lier au finding #2 — tout le workflow pipeline doit etre mieux guide | critique |
| 9 | UX/Refactor | Fusionner "Workflow Agents" et "Delegation History" en une seule section unifiee. L'user se fiche du type d'agent Claude sous le capot. Ce qu'il veut : (a) la liste des workflow agents (architect, coder, reviewer, tester, researcher) avec pastille verte si actif, (b) l'historique de delegation avec les noms des workflow agents (pas les noms internes Claude). Mapper les delegations detectees vers les noms des agents workflow definis dans `.claude/agents/`. Supprimer AgentsManager et AgentsSection separees, creer une seule section "Agents" | haute |
| 10 | Workflow | Claude fabrique des gates : a 09:19:47 il dit "Gates valides, on est en phase implementation" alors que l'utilisateur n'a JAMAIS valide de gate. Il s'auto-valide. Le mecanisme de gate doit etre mecanique (cote backend/hook), pas base sur la bonne volonte de Claude | critique |
| 11 | Bug | Agents paralleles cassent les npm installs : quand 4 agents codent en parallele, chacun fait son `npm install` independamment. Resultat : dexie n'etait pas installe dans le vrai node_modules du projet (conflit ERESOLVE + packages manquants). Les agents paralleles doivent soit serialiser les npm installs, soit travailler dans le meme contexte | haute |
| 12 | Workflow | Pas de phase review entre epic 1.1 et epic 1.2 : Claude enchaine directement "Build reussi → je passe a l'Epic 1.2" sans spawner de Reviewer, sans attendre de validation. Lie aux findings #8 et #10 | critique |
| 13 | Bug/Parser | Epic active affiche "EN COURS (0/6 taches)" alors que Claude dit 6/6 DONE — le parser de current-epic.md ne lit pas correctement les statuts des tasks. Soit Claude ecrit un format que le parser ne reconnait pas, soit le compteur de tasks ne se met pas a jour. Verifier le format ecrit par Claude vs le format attendu par reader.rs | haute |
| 14 | UX | Pipeline avance via validate_gate (l'user a clique manuellement en suivant Claude) mais Claude en mode free n'est pas au courant et ne synchronise pas son comportement. Le fire-and-forget message de validate_gate ne suffit pas — Claude doit etre force de s'aligner sur la phase actuelle du pipeline. Lie aux findings #8/#10 : le pipeline doit etre bidirectionnel (backend ↔ Claude) | haute |
| 15 | UX | Delegations montre massivement "general-purpose" comme nom d'agent — c'est le type interne Claude Code, pas le workflow agent. Lie au finding #9 : mapper vers les noms des workflow agents (architect, coder, etc.) | haute |
| 16 | Bug | Metriques "Tokens par phase" ne montre qu'une seule phase ("review" 52/15.9k) au lieu de toutes les phases traversees. Probablement lie au fait que le pipeline n'a pas ete reellement utilise (state.json pas mis a jour par Claude). A revalider une fois le pipeline mecanique en place | moyenne |
| 17 | UX | Metriques "Temps par agent" utilise les noms internes ("general-purpose" 24m, "architect" 2m) — devrait utiliser les noms des workflow agents. Lie au finding #9/#15 | moyenne |
